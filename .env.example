# Optional. Leave blank to let the manager generate a persistent 48-byte secret
# in ./data/manager.secret. Set explicitly when secrets are managed externally.
MANAGER_SECRET=

# Published host ports
MANAGER_PORT=8087
PROXY_PORT=8080

# Set to 1 only when the manager is served through HTTPS.
SECURE_COOKIES=0
